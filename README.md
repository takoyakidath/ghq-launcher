# GHQ Status Picker

Status bar button to open ghq repositories.

## Usage
- Click `GHQ` in the status bar (left).
- Pick a repo from the list.

## Requirements
- `ghq` installed and available in PATH.
